upstream twinscopes_django {
    server django:8000;
    keepalive 32;
}

upstream twinscopes_pgadmin {
    server pgadmin:80;
    keepalive 8;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}
